import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  // <React.StrictMode>
  <div style={{width: "100vw" ,backgroundColor: "#1f2124"}}>
    <App />
  </div>
  //</React.StrictMode>
);